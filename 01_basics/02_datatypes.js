"use strict" // treat all js version as newer version

// alert(3+3) // we are using node js not browser

let Name = "Aman", age = 22, isLoggedIn = false, state = null, city

// number => 2 to power 53
// bigint - for storing large values
// string => ""
// boolean => true/false
// undefined
// symbol => unique


// null => standalone value


// null is an object (means khali(ख़ाली) and it is a primitive datatype) and undefined is an undefined value

console.table([typeof Name, typeof age, typeof isLoggedIn, typeof state, typeof city])